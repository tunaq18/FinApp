
1.Tạo database FinManage trong SQL Server và chạy models/FinMagQuery.

2.Chạy:

npm install

npm start

3.Test API với Postman: đăng ký → đăng nhập → dùng token Authorization: Bearer <token> để gọi các endpoint.
